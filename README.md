# TI C6000 DSP Bootloader – AIS Protocol Compatible MCU Boot  
**MATLAB-Based TI-TXT Parsing → AIS Struct Generation → C Header Output**

---

## 📘 English Description

### Overview
This repository provides a complete workflow for booting **TI C6000 series DSPs (e.g., TMS320C672x)** using an external MCU through the **AIS (Application Image Script) boot protocol**.

In this project, all TI-TXT parsing and AIS command generation are performed **offline in MATLAB**.  
MATLAB processes the **TI-TXT hex file generated by the CCS C6000 Hex Utility**, converts the data into AIS command structures, and automatically exports them into a **C-compatible `.h` header file**.

The generated header file (e.g., `ais_image.h`) contains:
- AIS opcode structures  
- Load / Jump / Start / Fill command records  
- Program data blocks  
- AIS command sequence table  

This file is included in the MCU firmware and sent to the DSP over SPI or UART depending on the boot mode.

---

### 📄 Workflow Summary

#### 1. Build DSP application  
Compile your DSP code to generate:

```
project.out
```

#### 2. Convert DSP output to TI-TXT with AIS boot support  
```
hex6x –boot –o output.txt project.out
```

#### 3. MATLAB Offline Parsing  
The MATLAB script performs the following operations:

- Reads TI-TXT address and data blocks  
- Converts ASCII hex into binary words  
- Builds AIS opcode-based command records  
- Packs addresses, sizes, and payloads  
- Exports all AIS structures and data arrays into a **`.h` header file**  

#### 4. MCU Uses the Generated Header  
Simply include the file in your C project:

```c
#include "ais_image.h"
```

The MCU then transmits the AIS commands exactly as structured in the header file.

---

### 🧩 AIS Data Structures Used in `.h` Output

MATLAB generates the following C structures:

```c
typedef struct {
    uint32_t opcode;
    uint32_t address;
    uint32_t size;
} AIS_LoadCommand;

typedef struct {
    uint32_t opcode;
    uint32_t address;
} AIS_JumpCommand;

typedef struct {
    uint32_t opcode;
    uint32_t address;
    uint32_t value;
    uint32_t count;
} AIS_FillCommand;

typedef struct {
    uint32_t opcode;
} AIS_StartCommand;
```

Example generated entries:

```c
static const AIS_LoadCommand load_cmd_01 = {
    .opcode  = 0x58535901,
    .address = 0x00010000,
    .size    = 64
};

static const uint32_t load_data_01[16] = {
    0x12345678, 0x9ABCDEF0, ...
};
```

All content is generated automatically by MATLAB.

---

### 🔧 MCU Boot Process

1. MCU includes `ais_image.h`  
2. MCU reads AIS command records  
3. MCU sends AIS commands sequentially to the DSP  
4. DSP bootloader loads data, executes commands, and jumps to the application  

---

---

## 🇹🇷 Türkçe Açıklama

### Genel Bakış
Bu depo, **TI C6000 serisi DSP’lerin (TMS320C672x vb.)**, harici bir MCU ile **AIS (Application Image Script) boot protokolü** kullanılarak başlatılması için tam bir iş akışı sunar.

Bu projede ayrıştırma işlemleri MCU üzerinde yapılmaz.  
**TI-TXT → AIS dönüşümü tamamen MATLAB içinde offline olarak gerçekleştirilir.**

MATLAB:
- CCS Hex Utility tarafından oluşturulan **TI-TXT dosyasını okur**,  
- AIS komutlarına dönüştürür,  
- Tüm AIS struct’larını ve veri bloklarını **`.h` formatında dışa aktarır**.

Oluşturulan `ais_image.h` dosyası MCU yazılımına eklenerek DSP’ye gönderilecek tüm boot komutlarını içerir.

---

### 📄 İş Akışı Özeti

#### 1. DSP uygulamasını derle  
```
project.out
```

#### 2. Çıktıyı TI-TXT formatına dönüştür  
```
hex6x –boot –o output.txt project.out
```

#### 3. MATLAB Ayrıştırma ve `.h` Üretimi  
MATLAB scripti aşağıdaki işlemleri yapar:

- TI-TXT adres ve veri bloklarını okur  
- ASCII → binary dönüşümü yapar  
- AIS komut kayıtlarını oluşturur  
- Opcode, adres, boyut ve veri alanlarını paketler  
- Tüm komutları ve veri dizilerini **`.h` dosyasına yazar**  

#### 4. MCU Tarafında Kullanım  
MCU koduna:

```c
#include "ais_image.h"
```

eklemek yeterlidir.

MCU, bu komutları DSP’ye SPI veya UART üzerinden gönderir.

---

### 🧩 `.h` Dosyasında Oluşturulan AIS Yapıları

MATLAB tarafından oluşturulan C struct’ları:

```c
typedef struct {
    uint32_t opcode;
    uint32_t address;
    uint32_t size;
} AIS_LoadCommand;

typedef struct {
    uint32_t opcode;
    uint32_t address;
} AIS_JumpCommand;

typedef struct {
    uint32_t opcode;
    uint32_t address;
    uint32_t value;
    uint32_t count;
} AIS_FillCommand;

typedef struct {
    uint32_t opcode;
} AIS_StartCommand;
```

Örnek çıktı:

```c
static const AIS_LoadCommand load_cmd_01 = {
    .opcode  = 0x58535901,
    .address = 0x00010000,
    .size    = 64
};

static const uint32_t load_data_01[16] = {
    0x12345678, 0x9ABCDEF0, ...
};
```

Tüm içerik MATLAB tarafından otomatik üretilir.

---

### 🔧 MCU Boot Süreci

1. MCU `ais_image.h` dosyasını projeye dahil eder  
2. AIS komut dizisi MCU tarafından okunur  
3. AIS komutları sırasıyla DSP’ye gönderilir  
4. DSP bootloader komutları çalıştırır ve uygulamayı başlatır  

---

